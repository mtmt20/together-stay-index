# Together-Stay Index 프로젝트 안내

이 프로젝트는 컴퓨터 세션과 폰 세션 등 여러 Claude Code 세션을 오가며 작업합니다.
(위스키 파이프라인(`D:/00.py/whisky_hot/whisky-pipeline`)과는 완전히 분리된 별개
프로젝트/별개 git 저장소입니다 - 거기서 하는 작업과 여기서 하는 작업은 서로 영향
없습니다.)

## 세션 시작/종료 시 꼭 할 일 (충돌 방지의 핵심)

여러 세션(컴퓨터/폰)이 같은 로컬 폴더를 공유해서 작업하기 때문에, 두 세션이 "동시에"
뭔가를 하면 충돌합니다. Claude Code 세션끼리는 서로의 상태를 실시간으로 볼 수 없으므로,
**디스크에 있는 `HANDOFF.md`를 통해서만 순서를 맞춥니다** (동시 편집 대신 순차 인계):

- **작업 시작 전**: `HANDOFF.md`를 먼저 읽고 최근에 무슨 작업이 있었는지, 남은 작업이
  뭔지, 특히 "현재 실행 중인 것"(아래 참고) 항목을 확인하세요.
- **의미 있는 작업을 끝내면**: `HANDOFF.md`의 "최근 업데이트"에 날짜/세션 구분과 함께
  한 일을 짧게 추가하고, "진행 중 / 남은 작업"도 최신 상태로 고쳐주세요.
- **다른 세션이 이미 켜놨을 수 있는 것들을 함부로 다시 켜거나 끄지 마세요.** 시작하기
  전에 `docker compose ps`로 이미 떠 있는지 먼저 확인하고, 떠 있으면 재시작/재빌드하지
  말고 로그(`docker compose logs -f <service>`)만 보세요. 이미 떠 있는 스택을 모르고
  또 `docker compose up`하는 건 안전하지만(같은 호스트라 포트 충돌이 나면 그냥 실패함),
  `docker compose down`이나 `docker compose build`(이미지 재생성)는 다른 세션이 쓰고
  있는 걸 끊어버릴 수 있으니 HANDOFF.md에 "현재 실행 중" 표시가 없을 때만 하세요.
- **같은 파일을 두 세션이 동시에 고치지 마세요.** 한쪽 세션이 파일을 고치고 있는 중이면
  (터미널이 열려 있는 중이면) 다른 세션은 그 파일을 건드리지 말고 기다리거나 다른
  파일부터 작업하세요. Git으로 커밋해두면 다음 세션이 `git status`/`git log`로 최신
  상태를 바로 알 수 있습니다 - 의미 있는 변경 후에는 커밋해두는 걸 권장합니다.

## 사용자에게 뭔가 시킬 땐 링크+절차까지, 아주 쉽게 (모든 세션 공통 규칙)

키 발급, Snowflake 워크시트에서 SQL 실행, 외부 콘솔 설정처럼 **사용자가 직접 해야
하는 작업을 요청할 때는, 항상 접속 링크 + 클릭 순서 + 입력할 값까지 같이 적을 것.**
(2026-09-17 요청: "링크도 주고 매우 노인네 다루듯이 가이드해")
- 기술 용어(워크시트, 워크로드, 콘솔 등)는 최대한 안 쓰거나 옆에 쉬운 말로 풀어줄 것.
- "그 화면에서 설정" 대신 "화면 오른쪽 위 파란 버튼을 누르세요"처럼 버튼 위치·모양·
  색깔까지 묘사할 것.
- 한 번에 한 동작만 지시 (클릭 하나, 복사 하나, 붙여넣기 하나씩 번호 나눠서).
- 끝나면 뭘 보면 성공인지, 나한테 뭐라고 알려주면 되는지까지 적어줄 것.

## 현재 실행 중인 것 (세션 시작 시 이 섹션부터 확인)

`HANDOFF.md` 맨 위 "현재 실행 중" 줄을 보고, 아무것도 안 떠 있다고 되어 있어도
`docker compose ps`로 한 번 더 실제 상태를 확인하세요 (기록이 최신이 아닐 수 있음).

## Snowflake 비용 관리 (위스키 프로젝트와 동일한 이유의 규칙)

이 프로젝트도 트라이얼/저비용 Snowflake 계정을 쓰는 걸 전제로 합니다. 웨어하우스는
resume 1회당 최소 60초가 과금되므로:

- `docker compose run --rm tsi-dbt run`(dbt) 이나 Airflow DAG의 COPY INTO 태스크처럼
  Snowflake를 깨우는 작업을, 두 세션이 동시에 각자 트리거하지 마세요. 어느 세션이
  방금 돌렸는지 HANDOFF.md에 남기고, 다른 세션은 그 결과를 재사용하세요.
- dbt model 파일을 여러 개 고칠 계획이면 다 고치고 나서 마지막에 한 번만
  `dbt run`하세요 - 하나 고칠 때마다 돌리지 마세요.
- Streamlit 대시보드는 Gold 레이어를 1시간 캐시해서 조회하므로, 새로고침 버튼을
  눌러도 Snowflake가 매번 깨지 않습니다 (캐시 있는 동안은 안전하게 여러 번 봐도 됨).

## 호스트 메모리 공유 (위스키 스택과 같은 컴퓨터에서 돎, 2026-09-17 사고 이후 규칙)

이 프로젝트는 위스키 파이프라인(`D:/00.py/whisky_hot/whisky-pipeline`)과 **같은
Windows 컴퓨터, 같은 Docker Desktop**을 공유합니다. 포트/DB/git은 완전히 분리돼
있지만 **메모리는 공유**라서, 2026-09-17에 실제로 두 스택을 동시에 돌리다가 Docker
(WSL2)의 메모리 한도(당시 7.7GB)가 부족해 이 프로젝트의 Airflow 웹서버가
"No response from gunicorn master" 에러로 죽는 사고가 있었음. `.wslconfig`로 Docker
메모리를 12GB로 늘리고 웹서버 워커도 4→1로 줄여서 지금은 여유 있지만(합쳐서 약
2.7GB만 사용), 그래도:

- **안 쓸 때는 `docker compose stop`으로 내려두세요.** 이 프로젝트는 위스키처럼
  24시간 떠있어야 하는 프로덕션이 아니라 대회 데모용이라, 안 쓸 때 꺼두는 게
  위스키(진짜 서비스) 안정성에도 도움됨.
- 세션 시작 시 `docker stats`나 `docker ps`가 응답이 느리거나 안 되면, 자원 부족
  신호일 수 있으니 위스키 컨테이너 상태부터 먼저 확인할 것 (`docker ps` 전체 목록에서
  `whisky-*` 컨테이너들이 다 `Up`인지).
